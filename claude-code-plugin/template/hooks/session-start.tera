#!/usr/bin/env bash
# SessionStart hook for {{ plugin_name }} plugin

set -euo pipefail

cat <<EOF
{
  "additional_context": "You have {{ plugin_name }} skills available. Use the Skill tool to invoke them when relevant.",
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "You have {{ plugin_name }} skills available. Use the Skill tool to invoke them when relevant."
  }
}
EOF

exit 0
